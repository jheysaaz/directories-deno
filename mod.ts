/** Platform-specific base directories (cache, config, data, etc.) for the current user. */
export * as baseDirs from "./src/baseDirs.ts";

/** Platform-specific user directories (home, music, desktop, downloads, etc.) for the current user. */
export * as userDirs from "./src/userDirs.ts";

/** Platform-specific directories scoped to a specific application. */
export * as projectDirs from "./src/projectDirs.ts";
